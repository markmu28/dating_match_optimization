# 极简操作手册（现场版）

适用场景：现场快速出第一轮、第二轮、第三轮结果，支持 VIP、缺席、姓名映射。

## 0) 一次性准备

```bash
pip install -r requirements.txt
```

## 1) 先准备两个文件

- 偏好文件：`嘉宾偏好_第一轮.xlsx` / `嘉宾偏好_第二轮.xlsx` / `嘉宾偏好_第三轮.xlsx`
- 名单文件：`嘉宾名单.xlsx`（建议 `嘉宾名单` sheet，含 `嘉宾类型`、`编号`、`姓名`，可选 `英文名`、`别名`、`是否到场`）
- `ranking` 偏好表中，主体 `编号` 列与 `对象1ID/对象2ID` 都可直接填：ID、数字、中文名、英文名、别名
- 如有 `嘉宾姓名` 列，会优先按 `嘉宾姓名` 识别主体

## 2) 现场最常用命令（直接复制）

### 1. 第一轮 + VIP（推荐）

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --privileged-guests M1,F1 --export-xlsx --verbose
```

### 2. 第二轮 + VIP（基于第一轮结果）

```bash
python3 cli.py --round-two --first-round-file outputs/安排结果_第一轮.json --input 嘉宾偏好_第二轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --privileged-guests M1,F1 --export-xlsx --verbose
```

### 3. 第三轮（1v1 配对）

```bash
python3 cli.py --input 嘉宾偏好_第三轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --pairing-mode --export-xlsx --verbose
```

### 4. 第三轮 + VIP

```bash
python3 cli.py --input 嘉宾偏好_第三轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --pairing-mode --export-xlsx --verbose --privileged-guests M1,F1
```

### 5. 临时缺席（姓名/ID 混输）

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --absent-guests 张三,F8,M2 --export-xlsx --verbose
```

### 6. 缺席后仍强制 2男2女（不可行就报错）

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --absent-guests 李二 --strict-two-by-two --export-xlsx
```

### 7. VIP 用姓名指定

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --privileged-guests 王五,F3 --export-xlsx
```

### 8. 指定名单 sheet

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --guest-map-sheet 嘉宾名单 --export-xlsx
```

### 9. 仅解析检查（不求解）

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --dry-run-parse --verbose
```

### 10. 指定输出目录

```bash
python3 cli.py --input 嘉宾偏好_第一轮.xlsx --mode ranking --guest-map-file 嘉宾名单.xlsx --export-xlsx --output-dir outputs/活动A
```

## 3) 结果文件在哪里

- 第一轮：`outputs/安排结果_第一轮.json/.csv/.xlsx`
- 第二轮：`outputs/安排结果_第二轮.json/.csv/.xlsx`
- 第三轮配对：`outputs/安排结果_双人配对.json/.csv/.xlsx`

## 4) 三条现场规则（最重要）

- 第二轮不能和配对模式同时用：`--round-two` 不能配 `--pairing-mode`
- 配对模式下：男女差值必须 `<= 1`；若总人数为奇数且差值为 `1`，允许 1 组三人
- `text` 模式不支持缺席重编号；现场有缺席请用 `ranking` 模式
- 姓名若重名且无法唯一匹配，会提示告警；建议英文名/别名保持唯一
